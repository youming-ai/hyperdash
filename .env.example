# Hyperliquid Copy Trading Platform

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hyperdash
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=hyperdash
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Redis
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Auth
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=24h

# API Gateway
API_PORT=3000

# Web
WEB_PORT=5173
NEXT_PUBLIC_API_URL=http://localhost:3000

# Copy Engine
COPY_ENGINE_PORT=3006

# Hyperliquid API
HYPERLIQUID_API_URL=https://api.hyperliquid.xyz/info
HYPERLIQUID_WS_URL=wss://api.hyperliquid.xyz/ws

# Real-time feed (api-gateway)
# Comma-separated coins with static ctx+trades coverage (max ~12 on shared IPs;
# Hyperliquid throttles to ~13 subs/connection and 30 new connections/min per IP).
HL_FEED_COINS=BTC,ETH,SOL,HYPE,DOGE,XRP,SUI,ARB,OP,BNB,ADA,LINK
# Comma-separated whale wallet addresses to track taker flow (whale-flow panel).
HL_WHALE_ADDRESSES=
