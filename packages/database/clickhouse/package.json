{
  "name": "@hyperdash/clickhouse",
  "version": "1.0.0",
  "description": "ClickHouse database schemas and queries for HyperDash platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "migrate": "bun migrate.ts",
    "type-check": "tsc --noEmit",
    "query": "bun query-runner.ts"
  },
  "dependencies": {
    "@clickhouse/client": "^1.0.1",
    "@hyperdash/shared-types": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3"
  }
}
